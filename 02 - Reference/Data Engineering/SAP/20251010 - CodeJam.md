Devoberfest - https://community.sap.com/t5/devtoberfest-blog-posts/devtoberfest-2025-welcome/ba-p/14182817

Challenges to learn development in SAP. 

### Exercise 1

1. What are the advantages of using container images for development work?

Always have the same environments, and never need to use a host machine. Guarantee that everything is contained in a single environment. Like in a workspace from Fabric (ideally). 

2. What is the difference between the `@sap/cds` and `@sap/cds-dk` packages?

- Cds watch is short for "cds serve all --with-mocks --in-memory?", is a run time thing. 
- Authentication together with Business Roles. 
- Authentications: OData V4, OData V2 (but it's not possible in CAP) and REST. 

### Exercise 4 

- 404: my error
- 501: the source API error

Cds run: return error
cds watch: Don't get an error but will miss the information because we need to access the external server. 

/// mocking and external biding concept?

// Difference between design time and run time file

![[Pasted image 20251010133420.png]]