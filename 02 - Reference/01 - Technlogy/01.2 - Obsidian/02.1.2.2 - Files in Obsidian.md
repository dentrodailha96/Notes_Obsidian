**On file types:** Obsidian is not Markdown-only. It handles images, PDFs, audio, video, and `.canvas` files in a vault. It only _edits_ Markdown; other formats are stored and displayed. So that's not a constraint you need to worry about here.